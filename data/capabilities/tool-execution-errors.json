{
  "schema": "mcp-mirror/capability@3",
  "id": "tool-execution-errors",
  "title": "`isError`",
  "description": "The in-band flag marking a tool result as a tool execution error, which the spec expects clients to pass to the model so it can correct itself.",
  "question": "Can the agent tell a tool execution error from a successful result?",
  "spec": "https://modelcontextprotocol.io/specification/2025-11-25",
  "status": "stable",
  "categories": [
    "Tool results"
  ],
  "keywords": "isError,tool execution error,protocol error,self-correction,retry",
  "stats": {
    "crewai": {
      "1.15.1": "n #1"
    },
    "langchain": {
      "0.3.2": "a #2"
    },
    "openai-agents": {
      "0.22.0": "n #3"
    },
    "pydantic-ai": {
      "2.40.0": "a #4"
    },
    "mastra": {
      "1.17.3": "a #5"
    }
  },
  "notes": "Measured at every result boundary each adapter exposes: the value a direct invocation returns, and the value the framework's own agent path returns. No adapter delivers the specification's boolean on its default path. Three make the failure distinguishable, each through a different mechanism, and two return it as ordinary output an agent cannot tell from success. LangChain is the one adapter whose answer changes with the boundary, which is why its cell is marked as boundary-dependent rather than dropped.",
  "notes_by_num": {
    "1": "CrewAI: the adapter's tool reads only `result.content` and never `result.isError`, so a failed call returns the same bare string a successful one does. Identical at `BaseTool.run` and at `CrewStructuredTool.invoke`, which is the path a crew run takes.",
    "2": "LangChain: the answer depends on the boundary. `load_mcp_tools` builds the tool with `handle_tool_error=_handle_mcp_tool_error`, so an `isError` result raises an internal `ToolException` subclass that the handler turns back into tool output. Invoked with a plain argument dict the call returns those content blocks and nothing marks them as a failure. Invoked with a `ToolCall`, which is what an agent and `ToolNode` send, the same call returns a `ToolMessage` with `status=\"error\"`.",
    "3": "OpenAI Agents: `invoke_mcp_tool` reads `isError` only to decide whether `structuredContent` may be preferred, then emits the error text as an ordinary text output item. Identical at `MCPUtil.invoke_mcp_tool` and at `FunctionTool.on_invoke_tool`, which is what the agent loop calls.",
    "4": "Pydantic AI: the toolset's `tool_error_behavior` defaults to `'retry'`, which raises `ModelRetry` carrying the error text. Identical at `direct_call_tool` and at `call_tool`, which delegates to it; an agent run records that raise as a typed `RetryPromptPart` rather than as a tool return.",
    "5": "Mastra: the per-server `onToolError` option defaults to `'throw'`, so the tool action throws a `MastraError` carrying the error text. Set to `'return'` the same action resolves with the server's `CallToolResult`, `isError: true` included."
  },
  "boundary_dependent": [
    "langchain"
  ],
  "verdict": {
    "code": "a",
    "headline": "Distinguishable at three adapters of five, through three different mechanisms.",
    "detail": "LangChain marks the tool message `status=\"error\"`, Pydantic AI raises `ModelRetry`, and Mastra throws a `MastraError`; Mastra alone can be configured to hand back the flag itself, through its `onToolError: 'return'` server option. CrewAI and the OpenAI Agents SDK return the error text as ordinary output, so an agent reading the result cannot tell the call failed. One flag, five mechanisms, and the one an adapter gives you is not something the protocol lets you assume."
  },
  "usage_perc_y": null,
  "usage_perc_a": null,
  "usage_note": "Not published. Candidate source: count of public MCP servers using this capability. Not estimated, so the panel stays empty until the corpus is scanned.",
  "links": [
    {
      "url": "https://modelcontextprotocol.io/specification/2025-11-25/server/tools#error-handling",
      "title": "MCP Docs"
    }
  ],
  "known_issues": [],
  "why": "MCP separates protocol errors from tool execution errors precisely so a model can retry the second kind with better arguments. A framework that raises both as exceptions, or reports both as success, collapses that distinction and removes the recovery path the spec designed.",
  "reproduce": "uv run python scripts/measure_result_boundaries.py",
  "measured": {
    "run_date": "2026-09-18",
    "mcp_spec": "2025-11-25",
    "fixture": "fixtures/tricky_server.py"
  },
  "shown": true,
  "spec_label": "MCP 2025-11-25",
  "docs_url": "https://modelcontextprotocol.io/specification/2025-11-25/server/tools#error-handling",
  "docs_label": "MCP Docs"
}
